<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid x:Key="BoardSquare">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Ellipse Grid.Row="2" Style="{StaticResource PieceBaseStyle}" />
    <Border Grid.Row="0" Grid.RowSpan="3" Style="{StaticResource PieceBorders}">
      <Image Source="{Binding Path=ImagePath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource PieceStyle}"/>
    </Border>
  </Grid>

</ResourceDictionary>
  
<!--<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="BaseClass">
  <UserControl.Template>
    <ControlTemplate TargetType="UserControl">
      --><!-- This is where the Hosted Content will be placed --><!--
      <ContentPresenter ContentSource="Content"/>
    </ControlTemplate>
  </UserControl.Template>
</UserControl>-->